naftiko: 1.0.0-alpha2
info:
  label: Stability AI Stable Image Control API — Style
  description: 'Stability AI Stable Image Control API — Style. 1 operations. Lead operation: Generate an image with style
    transfer. Self-contained Naftiko capability covering one Stability Ai business surface.'
  tags:
  - Stability Ai
  - Style
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    STABILITY_AI_API_KEY: STABILITY_AI_API_KEY
capability:
  consumes:
  - type: http
    namespace: stable-image-control-style
    baseUri: https://api.stability.ai
    description: Stability AI Stable Image Control API — Style business capability. Self-contained, no shared references.
    resources:
    - name: v2beta-stable-image-control-style
      path: /v2beta/stable-image/control/style
      operations:
      - name: controlimagestyle
        method: POST
        description: Generate an image with style transfer
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.STABILITY_AI_API_KEY}}'
  exposes:
  - type: rest
    namespace: stable-image-control-style-rest
    port: 8080
    description: REST adapter for Stability AI Stable Image Control API — Style. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/v2beta/stable-image/control/style
      name: v2beta-stable-image-control-style
      description: REST surface for v2beta-stable-image-control-style.
      operations:
      - method: POST
        name: controlimagestyle
        description: Generate an image with style transfer
        call: stable-image-control-style.controlimagestyle
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: stable-image-control-style-mcp
    port: 9090
    transport: http
    description: MCP adapter for Stability AI Stable Image Control API — Style. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: generate-image-style-transfer
      description: Generate an image with style transfer
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: stable-image-control-style.controlimagestyle
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
